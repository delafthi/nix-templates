# Nix flake templates

Personal Nix flake templates for quick project initialization.

## Usage

Initialize a new project with a template:

```bash
nix flake init -t github:delafthi/nix-templates#template-name
```

List available templates:

```bash
nix flake show github:delafthi/nix-templates
```
